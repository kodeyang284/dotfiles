#!/bin/sh

if [ -n "$1" ]; then
    case "$1" in
    +) pactl set-sink-volume @DEFAULT_SINK@ +1000 ;;
    -) pactl set-sink-volume @DEFAULT_SINK@ -1000 ;;
    m) pactl set-sink-mute @DEFAULT_SINK@ toggle ;;
    *) echo "option error";;
    esac
fi
