#!/bin/bash

DOCS="$HOME/Documents"
ICON="📄"
prompt="📄 PDF"
prefix="$HOME/Documents/"

cmd=$(find "$DOCS" -type f -iname "*.pdf" -not -path "*/mydoc/*"| sed "s%$(echo $HOME/Documents/)%%g" | dmenu -i -p $prompt "$@")
while [[ -n $cmd ]]; do
	zathura "$prefix$cmd" 2>&1 >/dev/null
	exit 0
done
